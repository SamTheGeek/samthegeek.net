# Public client-side key for the lightbox map + local gallery scripts.
# Create separate restricted keys for local dev and production.
PUBLIC_GOOGLE_MAPS_EMBED_API_KEY=

# Optional separate key for Static Maps (if you want to split keys).
PUBLIC_GOOGLE_MAPS_STATIC_API_KEY=
